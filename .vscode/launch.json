{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Sentiment Analyzer (Dash)",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/app.py",
      "console": "integratedTerminal",
      "envFile": "${workspaceFolder}/.env"
    }
  ]
}